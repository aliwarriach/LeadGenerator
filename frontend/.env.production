# Production build: the API serves this SPA, so it is same-origin. A relative
# base URL means no CORS and no hardcoded deployed hostname. "/" is truthy, so
# api.js's localhost fallback is not taken.
VITE_API_BASE_URL=/
