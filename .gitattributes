# The container entrypoint is executed by bash inside a Linux image. Checked
# out with CRLF on Windows it fails at runtime with a "bad interpreter" error,
# so its line endings are pinned regardless of core.autocrlf.
*.sh text eol=lf
